# 7월 배포 워크플로우

작성일: 2026-07-11 (배포 목표: 2026-07-31)

## 1. 현재 상황

로그인/회원가입(이메일 인증 포함), 게시판 CRUD, 관리자 대시보드, 그리고 이번에 붙인 자격증 관리·출석 체크 모듈까지 동작하는 상태다. 스택은 Java 17 + Spring Boot 3.5 + MyBatis + MariaDB + Thymeleaf이고, 인증은 아직 Spring Security가 아니라 세션 기반 인터셉터(LoginInterceptor)로 처리하고 있다.

원래 그려둔 기획에는 GitHub 프로젝트 연동 면접질문 생성, 언어별 코딩테스트, AI 챗봇까지 5개 모듈이 있지만, 셋 다 외부 API 연동과 별도 설계가 필요한 무거운 기능이라 남은 3주 안에 전부 만들고 배포까지 끝내는 건 현실적으로 무리다.

## 2. 7월 배포 범위 결정

7월 목표는 "5개 기능을 다 갖춘 완성작"이 아니라 "실제로 접속 가능한 링크가 있는 서비스"로 잡는 걸 추천한다. 이력서에 넣을 링크는 기능이 4개든 2개든 살아있기만 하면 되고, 오히려 배포 파이프라인을 먼저 만들어두면 그 다음부터는 기능을 하나 만들 때마다 바로 반영되니까 개발 속도도 빨라진다.

그래서 7월 배포 범위는 지금 이미 동작하는 로그인·게시판·자격증·출석 4개 모듈로 확정하고, GitHub 연동 면접질문/코딩테스트/AI 챗봇은 8월 이후 스프린트로 넘긴다. 배포 자체가 인터뷰에서 "CI/CD, 리버스 프록시, 환경변수 분리를 직접 해봤다"고 말할 수 있는 좋은 소재이기도 하다.

## 3. 인프라 구성

EC2 한 대(Spring Boot 앱 구동)와 RDS MariaDB(신규 AWS 계정이면 프리티어 대상인지 콘솔에서 확인) 인스턴스를 분리해서 구성한다. 로컬에서 쓰던 `easy` 스키마를 RDS에 그대로 올리고, EC2 앞단에는 Nginx를 리버스 프록시로 붙여 80/443 포트를 8081(스프링 부트 기본 포트)로 넘긴다. HTTPS는 도메인을 하나 붙였다는 가정하에 Let's Encrypt(Certbot)로 무료 인증서를 발급받는다. 도메인이 없다면 일단 EC2 퍼블릭 IP + HTTP로 먼저 배포하고 도메인은 나중에 붙여도 된다.

DB 계정/비밀번호, 메일 발송 계정 같은 민감정보는 `application.yml`에 하드코딩하지 않고 지금처럼 환경변수(`DB_USERNAME`, `DB_PASSWORD`, `MAIL_USERNAME`, `MAIL_PASSWORD`)로 주입한다. EC2에서는 systemd 서비스 파일의 `Environment=` 항목이나 `/etc/environment`에 넣어 관리한다.

## 4. 배포 워크플로우 (실행 순서)

1. **RDS MariaDB 생성**: 리전은 서울(ap-northeast-2), 퍼블릭 액세스는 끄고 EC2와 같은 VPC 안에서만 접근 가능하도록 보안그룹을 구성한다. 생성 후 `easy` 스키마를 만들고 `easy_backup.sql`과 `sql/002_certification_attendance.sql`을 순서대로 실행해 테이블을 올린다.
2. **EC2 인스턴스 생성**: Amazon Linux 2023 또는 Ubuntu 22.04, t2.micro/t3.micro. 보안그룹에서 22(SSH)는 본인 IP만, 80/443은 전체 허용, 8081은 외부 차단(Nginx를 통해서만 접근하도록).
3. **EC2에 Java 17 설치 및 배포 디렉터리 준비**: `/opt/app` 같은 경로를 만들고 systemd 서비스 파일(`login-crud.service`)을 작성해 `java -jar app.jar`가 부팅 시 자동 실행되도록 등록한다.
4. **Nginx 설치 및 리버스 프록시 설정**: `location /` 블록에서 `proxy_pass http://127.0.0.1:8081;`로 연결. 도메인이 있으면 이 시점에 Certbot으로 HTTPS 인증서 발급.
5. **수동 배포로 1차 동작 확인**: 로컬에서 `./gradlew bootJar`로 빌드한 jar를 `scp`로 EC2에 올리고 systemd로 기동해서, RDS 접속과 화면 렌더링이 정상인지 먼저 눈으로 확인한다. 여기서 문제를 다 잡고 나서 자동화로 넘어가는 게 훨씬 편하다.
6. **GitHub Actions CI/CD 구성**: `main` 브랜치 push 시 `gradlew build` → jar 생성 → SSH로 EC2에 전송 → systemd 재시작까지 자동화하는 워크플로우 파일(`.github/workflows/deploy.yml`)을 추가한다. EC2 SSH 키와 RDS 접속정보는 GitHub Secrets에 등록해서 코드에 노출되지 않게 한다.
7. **배포 후 점검**: 회원가입(이메일 인증 포함), 로그인, 게시글 작성, 자격증 등록, 출석 체크가 실제 도메인/IP에서 끝까지 동작하는지 순서대로 테스트한다. Nginx 액세스 로그와 `journalctl -u login-crud`로 앱 로그도 같이 확인한다.

## 5. 주차별 일정

| 기간 | 목표 |
|---|---|
| 7/11 ~ 7/17 | 로컬에서 자격증·출석 모듈 최종 점검, RDS/EC2 인스턴스 생성, 수동 배포로 1차 동작 확인 |
| 7/18 ~ 7/24 | Nginx + HTTPS 설정, GitHub Actions CI/CD 파이프라인 구축, 배포 자동화 완료 |
| 7/25 ~ 7/31 | 실사용 시나리오 전체 테스트, 버그 수정, 도메인 연결 마무리, README에 배포 링크·아키텍처 설명 정리 |

## 6. 7월 배포의 완료 기준

주소를 몰라도 되는 사람이 도메인이나 IP로 접속해서 회원가입부터 로그인, 게시글 작성, 자격증 등록, 출석 체크까지 오류 없이 끝낼 수 있으면 7월 목표는 달성된 것으로 본다. 여기까지 끝나면 8월부터는 GitHub 연동 면접질문 생성 기능부터 이어서 붙이면 된다.
