server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/easy
    username: root
    password: 1234
    hikari:
      maximum-pool-size: 100

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.practice.logincrud