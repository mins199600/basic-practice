# 작업 일지 — 2026-07-14

오늘 하루 동안 자격증/출석 관리 모듈, 그리고 오답 가중치 기반 자격증 문제풀이 시스템을 새로 만들었다. 아래에 만든 순서대로 코드와 로직을 정리한다.

## 1. 자격증 관리 모듈 (CRUD)

### DDL — `sql/002_certification_attendance.sql`

`member` 테이블의 PK가 `INT(11)`이라 FK로 묶이는 `member_id` 컬럼도 반드시 같은 타입으로 맞춰야 했다. 처음에 `BIGINT`로 잡았다가 FK 생성이 실패해서 `INT(11)`로 고쳤다.

```sql
CREATE TABLE certification (
    id          INT(11)      NOT NULL AUTO_INCREMENT,
    member_id   INT(11)      NOT NULL,
    cert_name   VARCHAR(100) NOT NULL,
    exam_date   DATE         NULL,
    status      VARCHAR(20)  NOT NULL DEFAULT '준비중',
    memo        VARCHAR(500) NULL,
    created_at  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    deleted     TINYINT(1)   NOT NULL DEFAULT 0,
    PRIMARY KEY (id),
    KEY idx_certification_member (member_id, deleted),
    CONSTRAINT fk_certification_member FOREIGN KEY (member_id) REFERENCES member(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 구조

기존 `board` 모듈과 동일한 패턴(Dto → Mapper 인터페이스 → xml → Service → Controller)으로 만들었다.

- `certification/CertificationDto.java` — id, memberId, certName, examDate, status, memo, createdAt
- `certification/CertificationMapper.java` + `mapper/certification.xml` — 목록/단건 조회, 등록, 수정, soft delete, 전체 개수, 합격 개수
- `certification/CertificationService.java` — `getPassRate(memberId)`로 합격률(%) 계산 (등록 0개면 0%)
- `certification/CertificationController.java` — `/certification`(목록), `/certification/create`, `/certification/edit/{id}`, `/certification/delete/{id}`
- 화면: `templates/certification/list.html`, `create.html`, `edit.html`

수정/삭제 시 항상 `memberId`가 로그인한 세션과 일치하는지 확인해서, 남의 자격증을 수정/삭제하지 못하게 막아뒀다.

## 2. 출석 체크 모듈

같은 `sql/002_...` 마이그레이션에 `attendance` 테이블도 같이 넣었다. `UNIQUE KEY(member_id, attend_date)`로 하루 1건만 기록되게 강제했다.

```sql
CREATE TABLE attendance (
    id          INT(11)  NOT NULL AUTO_INCREMENT,
    member_id   INT(11)  NOT NULL,
    attend_date DATE     NOT NULL,
    created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    UNIQUE KEY uq_attendance_member_date (member_id, attend_date),
    CONSTRAINT fk_attendance_member FOREIGN KEY (member_id) REFERENCES member(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

`AttendanceService`에서 계산하는 값 세 가지:

- `hasAttendedToday(memberId)` — 오늘 이미 출석했는지
- `getCurrentStreak(memberId)` — 연속 출석일 수. 최근 90일 출석 날짜를 Set으로 만들고, 오늘부터(오늘 미출석이면 어제부터) 하루씩 거슬러 올라가며 존재 여부를 세는 방식
- `getAttendanceRate(memberId)` — 최근 30일 중 출석한 비율(%)

화면은 `templates/attendance/calendar.html`에서 최근 35일을 7×5 그리드로 그려서 GitHub 잔디 스타일로 보여준다. `/attendance/check-in`(POST)로 체크인하면 당일 1건만 insert된다.

## 3. 홈 대시보드 실데이터 연동

`home.html`에 원래 있던 "자격증 65%", "출석률 80%" 같은 하드코딩된 더미 숫자를 실제 값으로 바꿨다. `BoardController.home()`에 `CertificationService`, `AttendanceService`를 주입해서 매 요청마다 합격률/출석률/연속출석일을 계산해 모델에 담고, `home.html`의 stats-bar와 자격증 카드 배지에 그 값을 꽂았다. 홈 화면에 "오늘 출석하기" 버튼도 새로 추가했다.

## 4. board 테이블 오답노트 재활용 준비

`board` 테이블에 개발 중 넣었던 `qwe`, `123` 같은 테스트 게시글들을 지우는 SQL을 안내했다. `comment.board_id`가 `ON DELETE CASCADE`로 걸려있어서 `board`만 지우면 댓글도 같이 정리된다.

```sql
DELETE FROM board;
ALTER TABLE board AUTO_INCREMENT = 1;
```

## 5. 자격증 문제풀이 — 오답 가중치 출제 시스템 (오늘의 핵심 작업)

### 요구사항

자격증(피부자격증/SQLD/정보처리기사)을 클릭하면 문제가 1개씩 나오고, 라디오로 답을 고르면 즉시 채점. 정답이면 메시지 후 자동으로 다음 문제, 오답이면 해설을 보여주고 "다음 문제" 버튼을 눌러야 넘어간다. 그리고 틀린 문제일수록 다음에 더 자주 나오게 만드는 것.

### DDL — `sql/003_certification_question_and_stat.sql`

```sql
CREATE TABLE certification_question (
    id                INT(11)       NOT NULL AUTO_INCREMENT,
    certification_id  INT(11)       NOT NULL,
    question_text     VARCHAR(1000) NOT NULL,
    choice1           VARCHAR(255)  NOT NULL,
    choice2           VARCHAR(255)  NOT NULL,
    choice3           VARCHAR(255)  NOT NULL,
    choice4           VARCHAR(255)  NOT NULL,
    answer_no         TINYINT(1)    NOT NULL,
    explanation       VARCHAR(1000) NULL,
    created_at        DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    deleted           TINYINT(1)    NOT NULL DEFAULT 0,
    PRIMARY KEY (id),
    KEY idx_certification_question_cert (certification_id, deleted),
    CONSTRAINT fk_certification_question_cert
        FOREIGN KEY (certification_id) REFERENCES certification (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE member_question_stat (
    id                INT(11)  NOT NULL AUTO_INCREMENT,
    member_id         INT(11)  NOT NULL,
    question_id       INT(11)  NOT NULL,
    weight            INT      NOT NULL DEFAULT 1,
    correct_count     INT      NOT NULL DEFAULT 0,
    wrong_count       INT      NOT NULL DEFAULT 0,
    last_answered_at  DATETIME NULL,
    PRIMARY KEY (id),
    UNIQUE KEY uq_member_question (member_id, question_id),
    KEY idx_member_question_stat_member (member_id),
    CONSTRAINT fk_member_question_stat_member FOREIGN KEY (member_id) REFERENCES member (id),
    CONSTRAINT fk_member_question_stat_question FOREIGN KEY (question_id) REFERENCES certification_question (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

롤백은 자식 테이블(`member_question_stat`)부터 `DROP TABLE`.

### 가중치 upsert 로직 — `mapper/memberQuestionStat.xml`

정답 제출 시 weight -1(최소 1), 오답 시 +2. SELECT로 현재값을 먼저 읽어와서 계산하는 대신, `INSERT ... ON DUPLICATE KEY UPDATE` 한 문장으로 처음 제출(행 없음)과 재제출(행 있음)을 동시에 처리한다. `GREATEST()`로 최솟값 1을 항상 보장한다.

```sql
INSERT INTO member_question_stat
    (member_id, question_id, weight, correct_count, wrong_count, last_answered_at)
VALUES
    (#{memberId}, #{questionId}, GREATEST(1 + #{weightDelta}, 1), #{correctInc}, #{wrongInc}, NOW())
ON DUPLICATE KEY UPDATE
    weight           = GREATEST(weight + #{weightDelta}, 1),
    correct_count    = correct_count + #{correctInc},
    wrong_count      = wrong_count + #{wrongInc},
    last_answered_at = NOW()
```

`CertificationQuestionService.recordAnswer()`에서 정답이면 `weightDelta=-1, correctInc=1`, 오답이면 `weightDelta=2, wrongInc=1`을 넘겨서 이 upsert를 호출한다.

### 가중 랜덤 출제 로직 — `CertificationQuestionService.pickNextQuestion()`

1. `certificationQuestion.xml`의 `findCandidatesWithWeight`로 해당 자격증의 전체 문제를, `member_question_stat`과 LEFT JOIN해서 "이 회원 기준 현재 weight"까지 한 번에 가져온다. 통계가 없는 문제(한 번도 안 푼 문제)는 `COALESCE(s.weight, 1)`로 기본값 1을 준다.
2. 후보가 2개 이상이고 직전 문제 id가 있으면, 그 문제를 후보에서 제외한다. 제외한 결과가 0개면(=후보가 원래 1개였다는 뜻) 제외를 취소하고 원래 후보 전체를 쓴다 — "후보 1개면 연속 출제 허용" 조건.
3. 후보들의 weight 합을 구하고, `1 ~ 합계` 범위의 난수를 뽑아서 누적합 방식으로 문제를 하나 선택한다. weight가 큰(자주 틀린) 문제일수록 뽑힐 구간이 넓어서 더 자주 걸린다.

직전 문제 id는 DB가 아니라 `HttpSession`에 `lastQuestionId_{certificationId}_{memberId}` 키로 저장해서, 같은 사용자가 같은 자격증을 풀 때만 참조되게 했다.

### 화면 흐름 — `CertificationQuizController`

- `GET /certifications?memberId=1` — 그 회원의 자격증 목록. (자격증 관리 화면과는 별개의 진입점)
- `GET /certifications/{certificationId}/quiz?memberId=1` — 위 알고리즘으로 문제 하나를 뽑아 `mode=question`으로 렌더링. 문제가 하나도 없으면 `quiz-empty.html`.
- `POST /certifications/{certificationId}/quiz/submit` — 제출된 `questionId`/`selectedChoice`로 채점 후 통계 upsert, `mode=result`로 같은 `quiz.html` 템플릿을 다시 렌더링.
- `quiz.html` 안에서 정답이면 JS `setTimeout`으로 1.2초 뒤 자동으로 `GET /quiz`를 다시 호출해서 다음 문제로 넘어가고, 오답이면 해설을 보여주고 사용자가 "다음 문제" 버튼을 눌러야 넘어간다.

### 오늘 마지막 작업 — 자격증 목록에서 바로 문제풀이로 연결

`/certification`(자격증 관리 목록)에서 자격증명이 그냥 텍스트였는데, 이걸 클릭 가능한 링크로 바꿔서 `/certifications/{id}/quiz`로 바로 연결했다.

```html
<td class="title-cell">
    <a th:href="@{/certifications/{id}/quiz(id=${cert.id}, memberId=${memberId})}"
       th:text="${cert.certName}">정보처리기사</a>
</td>
```

## 6. 오늘 새로 만들거나 수정한 파일

**신규**
- `sql/002_certification_attendance.sql`, `sql/003_certification_question_and_stat.sql`
- `certification/` 패키지: `CertificationDto`, `CertificationMapper`, `CertificationService`, `CertificationController`, `CertificationQuestionDto`, `MemberQuestionStatDto`, `CertificationQuestionMapper`, `MemberQuestionStatMapper`, `CertificationQuestionService`, `CertificationQuizController`
- `attendance/` 패키지: `AttendanceDto`, `AttendanceMapper`, `AttendanceService`, `AttendanceController`
- `mapper/certification.xml`, `mapper/certificationQuestion.xml`, `mapper/memberQuestionStat.xml`, `mapper/attendance.xml`
- `templates/certification/list.html`, `create.html`, `edit.html`, `quiz-list.html`, `quiz.html`, `quiz-empty.html`
- `templates/attendance/calendar.html`
- `static/css/attendance.css`, `static/css/quiz.css`
- `DEPLOYMENT_PLAN.md`

**수정**
- `BoardController.java` — 홈 대시보드에 자격증/출석 데이터 주입
- `templates/home.html` — 하드코딩 값 제거, 실데이터 연동, 출석 버튼 추가
- `static/css/home.css` — `.dashboard-card`를 `<a>`로 써도 밑줄 안 생기게 처리

## 7. 알려진 제약 / 다음에 손볼 것

`/certifications` 계열 API가 로그인 세션이 아니라 `memberId`를 URL 파라미터로 직접 받는다. 로그인 자체는 `LoginInterceptor`가 막아주지만, 로그인한 사람이 URL의 `memberId`를 다른 사람 값으로 바꿔서 접근하는 건 막지 못한다. 실제로 여러 명이 쓰기 전에는 세션의 `memberId`와 파라미터 `memberId`가 같은지 검증하는 로직을 추가해야 한다.

그리고 이 문서에 있는 모든 코드는 로컬에서 `./gradlew build`로 직접 컴파일 검증을 아직 못 했다 (작업 환경에 네트워크 제한이 있어 Gradle wrapper를 못 받았음). 코드 패턴 자체는 기존 board/member 모듈을 그대로 따라 만들어서 문법 오류 가능성은 낮지만, 실제 빌드는 로컬에서 한 번 돌려봐야 한다.
